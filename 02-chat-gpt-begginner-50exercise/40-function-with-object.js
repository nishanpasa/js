console.log('40. **Function with Object**');
console.log('- Define a function `getFullName` that takes a `person` object with `firstName` and `lastName` properties and returns the full name. What is the full name for a person with the properties `firstName: "Alice"` and `lastName: "Johnson"`?');

function getFullName(firstName,lastName) {
    console.log(`Hello ${firstName} ${lastName}.`);
}
getFullName('Alice','Johnson');


